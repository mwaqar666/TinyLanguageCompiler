int main () {
    bool x;
    int y, z;
    int s[5];
    char t[4];

    y = 32766;
    z = 1;

    x = true;
    y = y + z;

    return s[4];

    t[3] = '5';

    while (x == true) { y = y + z; }

    if (x == true) { y = y + z; }

    else { y = y - z; }
}
